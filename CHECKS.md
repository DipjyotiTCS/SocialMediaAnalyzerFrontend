# Checks

- Added a dedicated `/login` route with a two-panel login layout inspired by the supplied reference image.
- Removed the Admin/User login selector from the login page as requested.
- Reused the existing application icon and the analyzer theme toggler on the login page.
- Added default demo credentials: `admin` / `admin123`.
- Kept `/`, `/analyzer`, and `/create-post` behavior unchanged behind the simple login flow.
- Verified React production build with `npm run build`.
